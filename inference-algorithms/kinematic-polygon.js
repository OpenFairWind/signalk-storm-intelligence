'use strict'
const {StormEngine}=require('../lib/storm-engine')
module.exports={id:'kinematic-polygon',name:'Kinematic polygon interception',description:'Persistent multi-frame cell tracking, robust velocity and vessel-path/polygon interception.',defaults:{enabled:true,weight:1},settingsSchema:{properties:{weight:{title:'Ensemble weight',type:'number',minimum:0,maximum:5,default:1}}},create({settings,common}){const engine=new StormEngine(common.stormConfig||{});return{id:'kinematic-polygon',name:'Kinematic polygon interception',version:'1',weight:Number.isFinite(Number(settings.weight))?Math.max(0,Math.min(5,Number(settings.weight))):1,capabilities:{tracking:true,geometry:true,pathInterception:true},infer(context){return engine.evaluate(context.snapshot,context.vessel)}}}}
